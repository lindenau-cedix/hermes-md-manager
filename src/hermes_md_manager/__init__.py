"""Hermes Agent Markdown Manager — a curation instrument for Hermes Agent's
persona / memory / skills tree.

The package is import-light: nothing imports web frameworks at import time,
nothing imports vendor modules at import time. ``paths`` is the only stateful
side effect on import.
"""
__version__ = "0.1.0"